{
  "private": true,
  "scripts": {
    "start": "npm --prefix frontend run start",
    "android": "npm --prefix frontend run android",
    "android:auto": "powershell -ExecutionPolicy Bypass -File .\\start-android.ps1",
    "web": "npm --prefix frontend run web",
    "backend:dev": "bun --cwd backend run dev",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@types/node": "^25.5.2",
    "@types/react": "~19.2.10",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "expo": "~55.0.14",
    "react-dom": "19.2.0"
  }
}
